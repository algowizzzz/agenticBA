from langchain_tools.tool4_metadata_lookup import init_db, fetch_all_metadata, create_structured_metadata_index; import json; db = init_db(); metadata = fetch_all_metadata(db); structured_index = create_structured_metadata_index(metadata); open('metadata_raw.txt', 'w').write(json.dumps(metadata, indent=2)); open('metadata_structured.txt', 'w').write(json.dumps(structured_index, indent=2)); print('Metadata saved to metadata_raw.txt and metadata_structured.txt')
